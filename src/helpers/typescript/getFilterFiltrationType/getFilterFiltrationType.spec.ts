import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests for helpers/getFilterFiltrationType (#78)'
)

describe('helpers/getFilterFiltrationType', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
