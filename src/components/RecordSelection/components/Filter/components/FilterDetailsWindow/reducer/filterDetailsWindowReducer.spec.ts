import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests on filterDetailsWindowReducer(#77)'
)

describe('Filter/FilterDetailsWindow/filterDetailsWindowReducer', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
