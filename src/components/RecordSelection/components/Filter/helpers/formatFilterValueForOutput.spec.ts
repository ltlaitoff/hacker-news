import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Rewrite tests for Filter/formatFilterValueForOutput (#77)'
)

describe('Filter/formatFilterValueForOutput', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
