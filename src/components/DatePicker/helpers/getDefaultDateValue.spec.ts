import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests for DatePicker/getDefaultDateValue helper'
)

describe('DatePicker/getDefaultDateValue', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
