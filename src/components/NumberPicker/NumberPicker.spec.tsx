import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo('Write tests for NumberPicker component (#75)')

describe('NumberPicker', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
