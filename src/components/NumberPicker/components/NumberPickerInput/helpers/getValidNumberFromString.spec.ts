import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests for NumberPicker/NumberPickerInput/getValidNumberFromString'
)

describe('NumberPicker/NumberPickerInput/getValidNumberFromString', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
