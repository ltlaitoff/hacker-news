import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo('Write tests on RecordComments component (#83)')

describe('RecordComments', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
