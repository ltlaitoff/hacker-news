import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests on RecordComments/ParentRecord component (#83)'
)

describe('RecordComments/ParentRecord', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
