import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests on RecordComments/CommentNode component (#83)'
)

describe('RecordComments/CommentNode', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
