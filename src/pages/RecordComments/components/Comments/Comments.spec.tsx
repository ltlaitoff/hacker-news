import ColoredConsoleLogTemplates from 'utils/colors'

ColoredConsoleLogTemplates.todo(
	'Write tests on RecordComments/Comments component (#83)'
)

describe('RecordComments/Comments', () => {
	it('-', () => {
		expect(true).toBe(true)
	})
})
